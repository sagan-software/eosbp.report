ReactDOMRe.hydrateToElementWithId(<App />, "app");

ReasonReact.Router.push("");

/* Temporary start */
/*
 let eos =
   Eos.Config.t(~httpEndpoint="http://node2.liquideos.com", ()) |. Eos.make;

 let _ = [%bs.raw {|window.eos = eos|}];

 let _ = [%bs.raw {|window.Eos = require('eosjs')|}]; */

/* Temporary end */
